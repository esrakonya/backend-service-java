app:
  environment: prod
  message: Gateway is running on Kubernetes Infrastructure

management:
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_URL}

logstash:
  host: ${LOGSTASH_HOST:marketplace-logstash}

logging:
  level:
    org.springframework.cloud.gateway: INFO
    org.esrakonya.backend: INFO