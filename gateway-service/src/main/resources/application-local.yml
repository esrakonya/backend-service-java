app:
  environment: local
  message: Gateway is running on Local

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
        - id: product-service
          uri: http://localhost:8082
        - id: inventory-service
          uri: http://localhost:8083

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
