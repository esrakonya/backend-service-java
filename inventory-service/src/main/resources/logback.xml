<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define the log format: Timestamp [Thread] Level Logger - Message -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Senior Move: Define different levels for Infrastructure vs Domain -->

    <!-- Silence noisy framework logs -->
    <logger name="org.springframework" level="WARN" />
    <logger name="org.hibernate" level="WARN" />
    <logger name="org.apache.kafka" level="WARN" />

    <!-- Keep our business logic visible -->
    <logger name="org.esrakonya.backend" level="INFO" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>